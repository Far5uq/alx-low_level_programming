#include "main.h"

/**
 * puts2 - function that print string
 * @str: pointer
 * Return: void
 */

void puts2(char *str)
{
int c, n, v = 0;
for (c = 0 ; str[c] != '\0' ; c++)
c++;
n = (v - 1) / 2;
for (c = n + 1 ; str[c] != '\0' ; c++)
_putchar(str[c]);
_putchar('\n');
}
