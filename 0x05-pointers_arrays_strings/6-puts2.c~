#include "main.h"

/**
 * puts2 - function that print string
 * @str: pointer
 * Return: void
 */

void puts2(char *str)
{
int c;
for (c = 0 ; str[c] != '\0' ; c++)
{
if (i % 2 == 0)
_putchar(str[c]);
}
_putchar('\n');
}
